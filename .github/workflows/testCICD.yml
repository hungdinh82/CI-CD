name: Test CICD
on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test_CICD_jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - run: echo "Hello, World!"